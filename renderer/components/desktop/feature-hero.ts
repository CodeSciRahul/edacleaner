/**
 * Shared min-height for feature-screen heroes.
 * Derived from PerformanceHero’s typical content height
 * (badge + title/message + stats + CTAs + hint, with sm:p-7 and lg side-by-side layout).
 */
export const featureHeroMinHeightClass = 'min-h-[17.5rem] sm:min-h-[19.5rem]'
