/** Fetch caps for dedicated storage list pages (overview keeps smaller defaults). */
export const STORAGE_PAGE_LARGE_FILES_LIMIT = 2_000
export const STORAGE_PAGE_DUPLICATES_LIMIT = 500

/** Rows / groups revealed per infinite-scroll page. */
export const STORAGE_LIST_PAGE_SIZE = 40
